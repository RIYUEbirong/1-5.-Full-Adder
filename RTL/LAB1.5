module fulladder(
input KEY1,
input KEY2,
input KEY3,
output LED0,
output LED1
);

wire sum;
wire carry;

assign sum = KEY1 ^ KEY2 ^ KEY3;
assign carry = (KEY1 & KEY2) | (KEY1 & KEY3) | (KEY2 & KEY3);

assign LED0 = sum;
assign LED1 = carry;

endmodule
